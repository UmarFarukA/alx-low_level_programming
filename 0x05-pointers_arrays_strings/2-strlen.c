#include <string.h>
#include "main.h"

/**
 * _strlen - Return the length of a string
 * @s: The string passed
 *
 * Return: length of string s
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
