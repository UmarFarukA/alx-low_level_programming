#include <stdio.h>

/**
 * main - Prints the name of program
 * @argc: Count the number of agrs
 * @argv: Array of args
 * Return: 0
 */
int main(int argc, char* argv[])
{
	printf("%s", argc[0]);
	return (0);
}
