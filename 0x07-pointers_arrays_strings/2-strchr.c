#include <string.h>
#include "main.h"

char *_strchr(char *s, char c);

/**
 * _strchr - Locates a c in s
 * @s: The string we will c in.
 * @c: the c we are locating
 * Return: Match poiter or null
 */
char *_strchar(char *s, char c)
{
	return (strchar(s, c));
}
