#include <stdio.h>
#include <string.h>
#include "main.h"

int _strlen_recursion(char *s);

/**
 * _strlen_recursion - Get length of string
 * @s: The string passed
 * Return: length of s
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
