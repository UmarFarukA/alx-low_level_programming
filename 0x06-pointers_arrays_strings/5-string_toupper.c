#include <ctype.h>
#include <string.h>
#include "main.h"

char *string_toupper(char *);

/**
 * string_toupper - Changeletter case t
 * Return: String
 */
char string_toupper(char *)
{
	char a[];
	int j;
	char k;

	for (j = 0; j < strlen(a); j++)
	{
		_putchar(toupper(a[j]);
	}
	_putchar('\n');
}
